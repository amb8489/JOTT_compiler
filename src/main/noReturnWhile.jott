#this will fail phase 3 for no return
#the loop does not have to run.

foo[x:Integer]:Integer{
    return x + 1;
}

main[]:Integer{
    Integer x = 0;
	while[x<10]{
	    x = foo[x];
		print[x];
		return x;
	}
}