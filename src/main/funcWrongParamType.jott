#this will fail phase 3 for wrong type of params in function call.

foo[x:Double]:Integer{
    return x + 1.0;
}

main[]:Integer{
    Double x = 0.1;
	while[x<10.0]{
	    x = foo[x];
		print[x];
	}
	return 0;
}